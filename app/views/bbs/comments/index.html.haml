%h1 comments

%article
  %header= @topic.title
  = @topic.body

%ol
  - @comments.each do |comment|
    %li
      %article
        %header= comment.title
        = comment.body

= paginate @comments

= form_for [bbs, @topic, Bbs::Comment.new] do |f|
  = f.text_field :title
  = f.text_area :body
  = f.submit
